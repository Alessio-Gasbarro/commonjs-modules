// Funzione che accetta firstName e lastName con Return
let firstName = 'Alessio';
let lastName = 'Gasbarro';

const fullname = (firstName, lastName) => {
    return firstName + ' ' + lastName
}

//Export const 'fullname'
module.exports = fullname;